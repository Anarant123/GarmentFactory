<Page x:Class="GarmentFactory.AddAccessoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GarmentFactory"
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="800"
      Title="AddAccessoriesPage">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock Text="Артикул Фурнитуры:"
                   FontSize="18"
                   Width="300"/>
        <TextBox x:Name="tbAA"
                 Width="300"
                 Style="{DynamicResource TextBoxStyle}"/>
        <Separator Height="20" Opacity="0"/>
        <TextBlock Text="Количество:"
                   FontSize="18"
                   Width="300"/>
        <TextBox x:Name="tbCA"
                 Width="300"
                 Style="{DynamicResource TextBoxStyle}"/>
        <Separator Height="20" Opacity="0"/>
        <Button x:Name="AddAccessories"
                Click="AddAccessories_Click"
                Content="Добавить"
                Width="200"
                Style="{DynamicResource ButtonStyle}"/>
    </StackPanel>
</Page>
